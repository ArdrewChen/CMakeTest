cmake_minimum_required(VERSION 3.10)
project(SORTLIB)
aux_source_directory(./ SRC)
include_directories(${HEAD_PATH})
set(LIBRARY_OUTPUT_DIRECTORY ${LIB_PATH}/lib)
add_library(${SORT_LIB} SHARED ${SRC})